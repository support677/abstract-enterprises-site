// Sticky pricing bar - shared across silos
// Each silo page has inline detect() in the page itself; this is a fallback no-op.
(function(){
  if (typeof window === 'undefined') return;
  // No-op stub - sticky pricing is now inlined per page
})();
