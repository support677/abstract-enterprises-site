# Buzzer Batch 12 — Locust Point, Longwood, Morris Park + silo-wide 800 sweep

**Cumulative.** Also carries the unpushed Cat6 silo (batches 3–5) and buzzer Batch 11. Deploy this alone.

Copy the **contents of this folder** into `C:\dev\abstract-enterprises-site`, replace when asked, commit, push.

## Pages rewritten

- `/door-buzzer-repair-locust-point-bronx-ny` — 4,586 words
- `/door-buzzer-repair-longwood-bronx-ny` — 4,557 words
- `/door-buzzer-repair-morris-park-bronx-ny` — 4,438 words

Melrose was skipped as you flagged — it already passed in Batch 2 with 7 schemas and 6 figures.

## Distinct theses

**Locust Point** — salt air is the whole story. A waterfront enclave where corrosion raises resistance long before it breaks a connection, so the signature symptom is *weak* rather than dead. Whole-path inspection rather than fixing only the joint that failed, plus flood-exposed transformer siting and seasonal timber movement on the strike.

**Longwood** — houses built for one family, answering for four. Historic district row houses divided over a century, where the entry system was extended rather than redesigned and the fault sits in the conversion-era junctions. Includes a direct treatment of what landmark designation actually restricts: the facade, not the wiring behind it.

**Morris Park** — the panel cannot keep up with who lives here. Medical-campus turnover sends directories stale faster than anywhere in the borough, so the job is frequently reconciliation plus setting the owner up to maintain it, rather than any repair at all.

## Silo-wide fix: 800 number removed

**27 other pages in the Bronx buzzer silo still carried (800) 486-0943** in their nav chrome and footers, against the standing rule that neighbourhood pages use the borough direct line only. All 27 swept to 646-490-0629. The silo now has zero occurrences of the 800 number.

That includes previously passed pages — Melrose, Fordham Heights and the rest of the Batch 1–10 work — which had been quietly non-compliant since the rule changed.

**One page left alone for you to decide on:** `door-buzzer-repair-pricing.html` carries 347-934-8335 (Brooklyn) in its nav. That is not the 800 line so it does not breach the rule, but a Bronx buzzer pricing page showing the Brooklyn number looks wrong. Tell me whether it should be 646 or whether that page is intended as sitewide.

## Duplicate scan

Body-only, excluding shared nav chrome and warranty constants, against the Batch 11 pages and the passed Fordham Heights and Melrose references:

**Worst pair: 9** — better than Batch 11's 10.

First build came in at 13; five genuinely duplicated prose sentences were rewritten to get there.

## Audit — all three pass

Title ≤60 · self-referencing canonical · 7 schema blocks, valid JSON · 21–22 H2 sections · exactly 6 unique figures, all resolving · FAQ 12=12 verbatim · 646 only, zero 800 · zero broken internal links · zero `.html` links · zero duplicate ids · 4.7/201 · NYS #12000287431 · registered in sitemap and redirects.

## Still outstanding from Batch 11

The other passed buzzer pages have 7 schemas but **no figures** — only Fordham Heights, Melrose and the six pages built in Batches 11–12 carry the image set. Worth a backfill pass across the rest of the silo.

## Files

| Path | Count |
|---|---|
| 6 buzzer pages rewritten (Batches 11–12) | 6 |
| 27 buzzer pages swept for the 800 number | 27 |
| 13 Cat6 pages (silo complete, unpushed) | 13 |
| `images/` | 99 |
| `sitemap.xml` · `_redirects` · `js/mega-nav.js` | 3 |
